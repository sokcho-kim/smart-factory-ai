# 작업일지 - 2026-05-18

## 작업 내역

### DB 파이프라인 구축
- PostgreSQL 컨테이너 기동 (punchline-db, port 5433)
- schema.sql 적용 (inspection_results, process_params, root_cause_analysis, action_log)
- server.py DB 모드 추가 (USE_DB=true 시 매 요청마다 SELECT)
- db.py 모듈 신규 작성 (read_inspections, read_process_params, count_inspections)
- **결과물**: `src/ai/db.py`, `src/ai/server.py` 수정

### 실시간 시뮬레이터
- simulator.py 작성 — CSV를 DB에 1건씩 INSERT, 타임스탬프 현재 시각 기준 재매핑
- 옵션: --speed(배속), --batch(묶음), --reset(초기화), --offset(이어하기)
- 1,440건 전체 적재 검증 완료
- **결과물**: `src/ai/simulator.py`

### 대시보드 자동 갱신
- useApi 훅에 refreshInterval 옵션 추가
- KPI 헤더 3초, 검사 스트림/타임라인 5초 자동 갱신
- 갱신 시 loading skeleton 미표시 (첫 로드만)
- **결과물**: `src/platform/dashboard/src/hooks/use-api.ts` 수정

## 이슈
- 시뮬레이터 offset 이어하기 시 타임스탬프 기준 불일치 — reset 후 전체 재적재로 해결
- 서버 구 프로세스 포트 점유 — taskkill로 수동 종료 필요

## 다음 할 일
- [ ] 예선 결과 확인 (5/19)
- [ ] 대시보드 비주얼 개선 (다크 테마, 게이지 등)
